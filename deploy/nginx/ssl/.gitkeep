# The panel's OWN TLS certificate (panel.crt / panel.key).
# Never shared with the customer website certificates.
#
# The installed panel keeps its certificate in /vkai-panel/ssl/, not here.
# This directory only exists so a manual nginx setup has a place to put one.
